#include "controller.h"
#include "unit.h"

Controller::Controller() {}
Controller::~Controller() {}

void Controller::deployUnits(int numberOfUnits, Vector2 pos) {
    for (int i = 0; i < numberOfUnits; i++) {
    }
}
